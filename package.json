{
  "name": "exomy",
  "private": true,
  "workspaces": [
    "packages/ControlCenter",
    "packages/RoverApp",
    "packages/Shared",
    "packages/i2c-bus"
  ],
  "scripts": {
    "lint": "yarn workspaces foreach --all run lint"
  },
  "packageManager": "yarn@4.10.3",
  "dependencies": {
    "i2c-bus": "workspace:*",
    "node-gyp": "^11.5.0"
  }
}
